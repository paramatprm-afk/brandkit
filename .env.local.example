# Copy this file to .env.local and fill in your key.
# Get an API key from https://console.anthropic.com/settings/keys
# This key is read server-side only (app/api/generate/route.ts) and is never exposed to the browser.
ANTHROPIC_API_KEY=
